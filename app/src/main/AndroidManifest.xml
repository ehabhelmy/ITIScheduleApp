<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jets.mad.itischeduleapp">

    <!-- ======================== PERMISSIONS ================================== -->
    <!-- Toqa -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- End of Toqa -->
    <!-- Ehab -->
    <!-- End of Ehab -->
    <!-- === End of Permissions === -->
    <application
        android:name=".AppController"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">

        <!-- ======================== ACTIVITIES ================================== -->
        <!-- *** SplashScreen Activity *** -->
        <activity android:name=".UI.Activity.SplashScreen">

            <!-- <intent-filter> -->
            <!-- <action android:name="android.intent.action.MAIN" /> -->


            <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
            <!-- </intent-filter> -->
        </activity>
        <!-- *** End of SplashScreen *** -->


        <!-- *** Login Activity *** -->
        <activity android:name=".UI.Activity.LoginActivity" />
        <!-- *** End of Login *** -->


        <!-- *** Main Activity*** -->

        <activity android:name=".UI.Activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- *** End of MainActivity *** -->

        <!-- *** ProfileEdit Activity *** -->
        <activity android:name=".UI.Activity.ProfileEditActivity" />
        <!-- *** End of ProfileEdit Activity *** -->

        <!-- *** ProfileEdit Activity *** -->
        <activity android:name=".UI.Activity.NotificationActivity"></activity>
        <!-- *** End of ProfileEdit Activity *** -->

        <!-- *** TestActivity *** -->
        <activity android:name=".UI.Activity.TestActivity" />
        <!-- *** End of Test *** -->


        <!-- === End of Activities === -->


        <!-- ======================== SERVICES ================================== -->
        <service
            android:name=".datalayer.Services.Token.TokenService"
            android:exported="true"
            android:permission="android.permission.BIND_JOB_SERVICE" />
        <service android:name=".datalayer.Services.Token.TokenServiceSupport" />

        <!-- === End of Services === -->

    </application>

</manifest>